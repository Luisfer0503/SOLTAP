[variables]
# La forma correcta de instalar extensiones PHP en Nixpacks (Railway)
NIXPACKS_PHP_EXTENSIONS = "bcmath,pdo_mysql,zip"

[phases.install]
cmds = [
    'composer install --optimize-autoloader --no-scripts --no-interaction --ignore-platform-reqs',
    'npm install'
]